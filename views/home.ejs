<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1"/>
  <title>Space Motors</title>
  <link rel="stylesheet" href="/public/styles.css">
</head>
<body>

  <%- include('header') %>

  <!-- HERO PRINCIPAL -->
  <section class="hero">
    <div class="hero__overlay"></div>
    <div class="hero__content container">
      <h1 class="hero__title">Tu próximo auto, hoy</h1>
      <p class="hero__subtitle">Autos seleccionados en Ciudad de México. Garantía, historial y trato directo.</p>
      <div class="hero__actions">
        <a class="btn btn-primary" href="/inventario">Ver inventario</a>
        <a class="btn" href="/como-comprar">¿Cómo comprar?</a>
      </div>
    </div>
  </section>

  <!-- LISTA RECIENTE -->
  <main class="container" style="padding:28px 0 48px;">
    <h2 class="section-title">Llegaron recién</h2>

    <section class="grid">
      <% (cars || []).forEach(c => { %>
        <article class="car-card">
          <a href="/auto/<%= c.slug %>" style="display:block;text-decoration:none;color:inherit">
            <img src="<%= c.cover || '/public/placeholder.jpg' %>" alt="<%= c.title %>">
            <div class="car-card-content">
              <h3><%= c.title %> <small>(<%= c.year || '' %>)</small></h3>
              <p class="price"><%= formatMoney(c.price || 0) %></p>
              <small class="meta"><%= c.city || '' %> • <%= (c.mileage || 0).toLocaleString() %> km</small>
            </div>
          </a>
        </article>
      <% }) %>

      <% if (!cars || cars.length === 0) { %>
        <p style="color:#aaa">Aún no hay autos publicados. Vuelve pronto 👀</p>
      <% } %>
    </section>

    <div style="text-align:center;margin-top:10px;">
      <a class="btn" href="/inventario">Ver todo el inventario</a>
    </div>
  </main>

  <%- include('footer') %>

</body>
</html>
