services:
  - type: web
    name: space-motors-app
    env: node
    buildCommand: npm install && npm run seed
    startCommand: node server.js
    plan: free
    envVars:
      - key: ADMIN_USER
        value: admin
      - key: ADMIN_PASS
        value: password
      - key: WHATSAPP_PHONE
        value: 52536343619
